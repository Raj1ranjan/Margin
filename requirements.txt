PyQt6>=6.5.0
PyPDF2>=3.0.0
PyMuPDF>=1.22.0
# Ghostscript is a system dependency, not a Python package
# Install via: sudo apt-get install ghostscript (Linux)
# or: brew install ghostscript (macOS)
# or download from https://www.ghostscript.com/ (Windows)
